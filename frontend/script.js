function login() {
  var email = document.getElementById("login-email").value;
  var password = document.getElementById("login-password").value;

  // Send login request to the backend server
  // This part would be handled by your backend implementation
  // You would typically use fetch or XMLHttpRequest to send the data to your backend
}

function signup() {
  var email = document.getElementById("signup-email").value;
  var password = document.getElementById("signup-password").value;

  // Send signup request to the backend server
  // This part would be handled by your backend implementation
  // You would typically use fetch or XMLHttpRequest to send the data to your backend
}
